{
  "name": "kh-volonteer-bot",
  "private": true,
  "version": "0.0.0",
  "types": "module",
  "scripts": {
    "dev": "nodemon",
    "build": "rimraf ./dist && npx tsc && tsc-alias",
    "start": "npm run build && cross-env NODE_ENV=production node dist/main"
  },
  "devDependencies": {
    "@types/node": "^18.14.2",
    "nodemon": "^2.0.20",
    "rimraf": "^4.1.2",
    "tsc-alias": "^1.8.2",
    "tsconfig-paths": "^3.14.2",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "@fastify/autoload": "^5.7.1",
    "@fastify/env": "^4.2.0",
    "@grammyjs/conversations": "^1.1.1",
    "axios": "^1.3.4",
    "fastify": "^4.13.0",
    "grammy": "^1.14.1"
  }
}
